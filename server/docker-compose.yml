services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: kyc
      POSTGRES_PASSWORD: kyc
      POSTGRES_DB: kyc_orchestrator
    ports:
      - "5432:5432"
    volumes:
      - kyc_postgres_data:/var/lib/postgresql/data

volumes:
  kyc_postgres_data:
